# Backend

NestJS + Prisma API for SWA6Y5TAN.

```bash
npm ci
npm run build
npm run start:prod
```

Environment and deployment instructions are in the root `README.md`.

Production database changes use:

```bash
npx prisma migrate deploy
```
